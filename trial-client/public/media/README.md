# Drop your demo video here

Put your real pushup demo (landscape phone footage) at:

    public/media/demo-pushups.mp4

The homepage "Watch it count real reps" section appears automatically when the
file exists, and the frame takes the video's own aspect ratio — no cropping, no
letterboxing. If the file is missing, the section hides itself entirely.
